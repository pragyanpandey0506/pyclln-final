# Linear Net

`linear_net/` is the active home for reorganized linear and linear-resistor
language-model work.

Current structure:

- `lin_lang_model/`
  - linear and linear-resistor language trainers, launchers, and result trees

Older linear entry points still exist under `lang_model/` as compatibility
wrappers so existing commands do not break.
